# Separate from requirements.txt so production installs (and the Docker image,
# whenever that gets built) don't pull in test/lint tooling they'll never use.
-r requirements.txt

ruff==0.8.6
black==24.10.0
pytest==8.3.4
httpx==0.28.1
