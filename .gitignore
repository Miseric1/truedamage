# Env files (never commit real secrets — .env.example files ARE committed)
.env
*.env
!.env.example

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Backend and frontend have their own .gitignore for stack-specific artifacts
