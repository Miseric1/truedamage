# Overrides for docker-compose.yml. Copy to .env if you want non-default
# credentials locally. Safe to commit this file (it's an example, not real
# secrets) — the real .env is gitignored.

POSTGRES_USER=lol_admin
POSTGRES_PASSWORD=changeme_local_only
POSTGRES_DB=lol_analytics
